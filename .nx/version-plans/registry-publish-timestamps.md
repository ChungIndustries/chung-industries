---
cpm-registry: minor
cpm-web: minor
---

Package responses now expose publish timestamps: `createdAt` on each version and on the package itself (first publish). The website renders them as a relative "published X ago" in the package index rows and in each package's versions list.
