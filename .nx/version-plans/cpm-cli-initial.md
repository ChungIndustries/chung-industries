---
cpm-cli: minor
---

Initial cpm client for CC:Tweaked: `install`, `remove`, `update`, `list`, and `search` against the registry, with server-side dependency resolution, sha256-verified bundle downloads, a flat `/cpm/packages` store with shell-path shims, and a self-contained bootstrap installer.
