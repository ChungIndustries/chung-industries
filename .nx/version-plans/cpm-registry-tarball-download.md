---
cpm-registry: minor
---

Add a tarball download endpoint (`GET /packages/:name/:version/dist/tarball`) that serves the gzipped tarball for a published version, completing the publish/download loop.
