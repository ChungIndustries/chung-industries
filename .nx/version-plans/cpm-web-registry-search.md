---
cpm-web: minor
---

The package index now searches through the registry's search endpoint instead of downloading every package and filtering in the browser. Results arrive in pages with a "Show more" button, name matches rank first, and the landing page's "Try:" names come from the same endpoint.
