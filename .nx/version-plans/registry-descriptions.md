---
cpm-registry: patch
---

The API reference now reads in plain language: every endpoint, response, and field description was rewritten to say what it does in ordinary sentences, without repeating what the spec already shows.
