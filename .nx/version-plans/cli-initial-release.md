---
cli: minor
---

Initial release of `cli`, a library for building command-line interfaces on CC:Tweaked. A program describes its commands, arguments, and options once, and the library parses and validates the input, dispatches to the right handler, and generates the help text from those declarations.
