name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  affected:
    name: Affected (typecheck / lint / test / build)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup
        uses: ./.github/actions/setup

      - name: Derive affected SHAs
        uses: nrwl/nx-set-shas@v5

      # verify-generated regenerates committed artifacts (OpenAPI spec, Worker Env
      # types) and fails on drift, so the checks only run for affected projects.
      - name: Typecheck, test, build, verify generated files (affected)
        run: pnpm exec nx affected -t typecheck test build verify-generated

      - name: Lint (affected)
        run: pnpm exec nx affected -t lint -- --format=github

  format:
    name: Format (oxfmt)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup
        uses: ./.github/actions/setup

      - name: Check formatting
        run: pnpm format:check
