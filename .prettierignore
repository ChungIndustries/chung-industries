# Per-project changelogs, generated by `nx release` (tools/release.mjs).
**/CHANGELOG.md

# Worker Env types, generated from wrangler.toml by `wrangler types` (pnpm gen-types).
# Verified instead by each Worker project's `verify-generated` target.
apps/*/worker-configuration.d.ts
